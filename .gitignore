# --- Rust Specific ---
# The target directory is where all build artifacts (binaries, library files) are stored.
# This can become very large and should never be committed.
/target/

# --- Cargo Specific ---
# These files are generated by Cargo and are usually not committed in libraries,
# but MUST be committed in binary/executable projects to ensure reproducible builds.
# If this is a library project, you might want to uncomment the line below.
# Cargo.lock

# --- IDEs and Editors ---
# JetBrains (IntelliJ, CLion, RustRover)
.idea/
*.iml

# Visual Studio Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Vim/Neovim
.*.swp
.netrwhist
Session.vim

# --- OS Specific ---
.DS_Store
Thumbs.db

# --- Miscellaneous ---
# Environment variables (often containing secrets)
.env
.env.local

# Benchmarking/Profiling output
perf.data
perf.data.old